#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string to stdout followed by a new line
 * @str: the string to be printed
 *
 * Return: no return
 */
void _puts(char *str)
{
	_puts("str");
}
