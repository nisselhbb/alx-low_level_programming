#include "main.h"

/**
 * _abs - Enrty point
 * @n: the integer
 *
 * Return: the absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		_putchar('n');
		return (n);
	}
	else
	{
		_putchar("n * -1");
		return (n * -1);
	}
	_putchar('\n');
}
